import styled from "styled-components";

const Moodbox = styled.p`
    font-size: 19px;
    color: #fff;
`
export const Mood = () => {
    return (
        <Moodbox>
         {"{ Think I'm turning into a ghost }"}
        </Moodbox>
    );
};